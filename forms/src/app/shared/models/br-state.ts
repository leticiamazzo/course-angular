// 19FM - Diferença entre classe e interface. Na classe poderia ter métodos com corpo, construtor, implementação de uma classe normalmente. Interface tem apenas a assinatura do que precisamos ter em um objeto que vai implementá-la. Conceitos de Orientação à objeto

export interface BrState {
  id: number;
  initial: string;
  name: string;
}
